export interface UserAuth {
    uid: string;
    email: string;
    password: string;
    accessToken: string;
}

export interface  UserSignIn {
    uid: string;
    email: string;
    password: string;
    accessToken: string;
}

export interface UserSignUp {
    uid: string;
    email: string;
    password: string;
    accessToken: string;
}